name: Generate Snake Animation

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: Platane/snk@v3
        with:
          github_user_token: ${{ secrets.GITHUB_TOKEN }}
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark

      - uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```
- Click **Commit changes** ✅

**3️⃣ Run it manually**
- Go to **Actions** tab in your repo
- Click **"Generate Snake Animation"** on the left
- Click **"Run workflow"** button → **"Run workflow"** (green button)
- Wait 1–2 minutes ⏳

**4️⃣ Verify it worked**

Check this URL exists after it runs:
```
https://github.com/manthansakhare2-ms/manthansakhare2-ms/tree/output
